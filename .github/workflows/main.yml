name: 'Social Icon build'

on:
  workflow_dispatch:
  schedule:
    - cron: '* */2 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install and Update README.md
	run: |
	  npm install
	  npm run build
