name: 'Social Icon build'

on:
  workflow_dispatch:
  schedule:
    - cron: '* */2 * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: install
        run: yarn install
        
      - name: Build
        run: yarn run build

